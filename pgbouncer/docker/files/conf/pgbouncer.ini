[databases]
DB_NAME = host=PG_SERVICE_NAME port=PG_SERVICE_PORT dbname=DB_NAME

[pgbouncer]
listen_port = PG_PORT
listen_addr = *
auth_type = trust
auth_file = /files/users/users.txt
logfile = /tmp/pgbouncer.log
pidfile = /tmp/pgbouncer.pid
admin_users = PG_ADMIN